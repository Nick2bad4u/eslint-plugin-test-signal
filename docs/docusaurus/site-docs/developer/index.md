# Developer Guide

Source code lives in `src/`, rules live in `src/rules/`, and rule docs live in
`docs/rules/`.

Run the standard verification commands from the repository root:

```sh
npm run build
npm run typecheck
npm run test
```
